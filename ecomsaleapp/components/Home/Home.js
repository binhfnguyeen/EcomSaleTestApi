import { Text } from "react-native-paper";
import { SafeAreaView } from "react-native-safe-area-context"

const Home = () => {
    return (
        <SafeAreaView>
            <Text>Đây là trang chủ Ecomsale</Text>
        </SafeAreaView>
    )
}

export default Home;